#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
int main()
{   float a,b,sum;
    printf("Enter the first number:");
    scanf("%f",&a);
    printf("Enter the second number");
    scanf("%f",&b);                                                                                           
    sum=a*b;
    printf("The product is:%.d",c);
    getch();
    return 0;
}